export default (variables: any, classes: any) => ({
    ...classes,

    DateTimeFieldView__press: {
        paddingHorizontal: 16,
        paddingVertical: 8,
        backgroundColor: 'rgba(0, 0 ,0, 0.6)',
        borderRadius: 16,
    },

    DateTimeFieldView__text: {
        color: '#FFFFFF',
        fontSize: 16,
    },
});
